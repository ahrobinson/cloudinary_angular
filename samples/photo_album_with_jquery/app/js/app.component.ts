import { Component } from '@angular/core';
@Component({
    moduleId: module.id,
    selector: '.content',
    template: `
        <router-outlet></router-outlet>
    `
})
export class AppComponent {
}
